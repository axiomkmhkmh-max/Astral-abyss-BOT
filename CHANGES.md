# تغییرات این نسخه

## ۱. سیزن‌پسِ PvP فصلی (pvp.py, weekly_rewards.py, pvp_handlers.py, database.py)
- شماره‌ی فصل الان نگه داشته می‌شه (`pvp_season_meta` تو system_col) و هر هفته +۱ می‌شه.
- به‌جای اینکه فقط ۳ نفرِ اولِ کل سرور جایزه بگیرن، **هر کسی که اون فصل بازی کرده** بر اساسِ
  لیگِ نهایی‌ش (Bronze تا Eternal) جایزه‌ی Zen می‌گیره؛ تاپ ۳ یه بونوسِ اضافه هم روش می‌گیرن.
- هر بازیکن `pvp_season_history` (۱۰ فصلِ آخر: لیگ/امتیاز/رتبه/جایزه) و یه بجِ
  `pvp_last_season_league` داره که تو `/stats` نشون داده می‌شه.
- دستورِ جدید `/pvpseason`: وضعیتِ فصلِ جاری + جایزه‌ی تخمینی + ۳ فصلِ آخر.
- فقط تاپ ۱۰ پیامِ مستقیمِ پایانِ فصل می‌گیرن (نه اسپمِ همه‌ی سرور).

## ۲. واکنشِ کاتانا به Resonance/داستان (katana_resonance.py — فایلِ جدید، quest_handlers.py)
- کاتانا الان به مسیرِ اخلاقیِ داستان (Light/Void/تعادل) هم واکنش نشون می‌ده، نه فقط کیل/دث.
- واکنش بر اساسِ ۱۲ تیپِ شخصیتیِ موجودِ کاتانا (katana_personality.py) فرق می‌کنه.
- فقط وقتی تغییر واقعاً قابل‌توجه باشه (رد شدن از آستانه‌ی Light/Void یا جهشِ بزرگ) دیالوگ
  نشون داده می‌شه — نه بعدِ هر انتخابِ ریز داستان.
- به‌صورتِ toast (`cb.answer`) بعدِ choice نودها و بعدِ جریمه‌ی memory_check نشون داده می‌شه.

## ۳. آنتی‌چیت رفتاری پیشرفته (anti_farm.py, mob_combat.py, combat_handlers.py, database.py)
- علاوه بر سقفِ نرمِ روزانه‌ی قبلی، الان فاصله‌ی زمانیِ بینِ ۱۲ اکشنِ آخرِ هر بازیکن ثبت می‌شه.
- اگه هم میانگینِ فاصله سریع‌تر از سرعتِ انسانی باشه (< 1.2s) هم فاصله‌ها خیلی یکنواخت باشن
  (واریانسِ نسبی < 0.12 — یعنی الگوی ماشینی/اسکریپتی)، یه لاگِ هشدارِ جدا برای ادمین ثبت می‌شه.
- فقط لاگ می‌کنه؛ خودکار بلاک نمی‌کنه (تصمیمِ نهایی دستِ ادمینه).

## نکته
هیچ فایلی حذف یا بازنویسیِ کامل نشده — همه‌ی افزودنی‌ها روی همون معماریِ موجود (system_col،
apply_player_defaults، الگوهای idempotent) سوار شدن. قبل از دیپلوی روی Railway حتماً یه تست
دستی از `/pvpseason`، یه انتخابِ داستانی، و چند تا `/attack` پشتِ‌هم بزن.
